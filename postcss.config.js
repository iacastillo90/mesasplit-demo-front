// postcss.config.js — cadena PostCSS para procesar las directivas de Tailwind
// Tailwind genera las utilidades y autoprefixer agrega prefijos de navegador.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
